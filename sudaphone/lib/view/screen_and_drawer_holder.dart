import 'package:flutter/material.dart';

class ScreenAndDrawerHolder extends StatelessWidget {
  
  const ScreenAndDrawerHolder({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}