import 'package:flutter/material.dart';

class BuildFormSignUp extends StatelessWidget {
  const BuildFormSignUp({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}